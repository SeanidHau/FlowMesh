# 安全策略

## 支持范围

安全修复以 `main` 分支的最新提交为准。部署时应使用已经通过 CI、Security、Trivy 和 Cosign 校验的完整提交 SHA，不要使用未审核的版本标签。

## 报告安全问题

请通过 GitHub 的私密漏洞报告或 Security Advisory 联系维护者。不要在公开 Issue、Pull Request、日志或聊天记录中披露可复现漏洞的凭据、Token、证书或生产地址。

报告应尽量包含：

- 受影响的提交、组件或配置；
- 可重复的步骤和最小化复现信息；
- 影响范围与可能的攻击条件；
- 已知的临时缓解措施。

维护者会先确认影响范围，再制定修复、回归测试和披露计划。报告中不要提交真实 Secret 或包含敏感数据的生产证据包。

## 安全基线

- JWT 签名密钥、数据库密码、Redis 凭据、RocketMQ 凭据、对象存储密钥和 Webhook 签名密钥只能通过受保护的运行时 Secret 注入。
- 生产环境必须启用 PostgreSQL、Redis、RocketMQ 和对象存储的传输安全配置，并完成对应的目标平台验收。
- PostgreSQL RLS、服务账号最小权限、事件幂等和审计表追加写入是安全边界的一部分，修改时必须补充测试。
- 生产镜像必须使用不可变基础镜像 digest，并通过漏洞扫描和签名校验。
- 发现凭据泄露时，应立即撤销或轮换凭据，并按运行手册保留处置记录。

详细的认证、授权、多租户隔离、对象存储和日志要求见 [安全规范](docs/security.md)。
