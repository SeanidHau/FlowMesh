# Docker Compose 本地环境

本目录将保存 FlowMesh 的本地基础设施与服务编排文件。

计划包含 PostgreSQL、Redis、MinIO、RocketMQ、Camunda 8、可观测组件和后端服务。具体 Compose 文件将在基础设施实现阶段加入。

本地环境使用仓库根目录的 `.env`。先复制 `.env.example`，再填写本地凭据。不要提交 `.env`。
