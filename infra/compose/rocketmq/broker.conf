# 作用：本地单 Broker 配置，供 Compose 网络内的 Java 服务访问。
brokerClusterName = DefaultCluster
brokerName = flowmesh-broker
brokerId = 0
brokerRole = ASYNC_MASTER
flushDiskType = ASYNC_FLUSH
brokerIP1 = rocketmq-broker
