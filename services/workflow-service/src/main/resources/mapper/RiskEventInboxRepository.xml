<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowmesh.workflow.repository.RiskEventInboxRepository">
  <select id="existsById" resultType="boolean">
    SELECT EXISTS (
      SELECT 1 FROM workflow_risk_event_inbox WHERE event_id = #{eventId, jdbcType=OTHER}
    )
  </select>

  <insert id="insert">
    INSERT INTO workflow_risk_event_inbox (event_id, tenant_id, application_id)
    VALUES (#{eventId, jdbcType=OTHER}, #{tenantId}, #{applicationId, jdbcType=OTHER})
  </insert>
</mapper>
