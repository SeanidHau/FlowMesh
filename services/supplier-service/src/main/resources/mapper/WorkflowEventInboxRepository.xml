<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowmesh.supplier.repository.WorkflowEventInboxRepository">
  <insert id="save" parameterType="com.flowmesh.supplier.domain.WorkflowEventInbox">
    INSERT INTO supplier_workflow_event_inbox (event_id, tenant_id, aggregate_id, created_at)
    VALUES (#{eventId, jdbcType=OTHER}, #{tenantId}, #{aggregateId, jdbcType=OTHER}, #{createdAt})
  </insert>

  <select id="existsById" resultType="boolean">
    SELECT EXISTS (
      SELECT 1 FROM supplier_workflow_event_inbox WHERE event_id = #{eventId, jdbcType=OTHER}
    )
  </select>

  <select id="count" resultType="long">
    SELECT COUNT(*) FROM supplier_workflow_event_inbox
  </select>

  <select id="countByAggregateId" resultType="long">
    SELECT COUNT(*) FROM supplier_workflow_event_inbox
    WHERE aggregate_id = #{aggregateId, jdbcType=OTHER}
  </select>
</mapper>
