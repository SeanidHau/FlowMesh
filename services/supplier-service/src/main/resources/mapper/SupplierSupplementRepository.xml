<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowmesh.supplier.repository.SupplierSupplementRepository">
  <insert id="insert" parameterType="com.flowmesh.supplier.domain.SupplierSupplement">
    INSERT INTO supplier_application_supplements
      (id, tenant_id, application_id, round_no, submitted_by, comment, created_at)
    VALUES (#{id, jdbcType=OTHER}, #{tenantId}, #{applicationId, jdbcType=OTHER}, #{roundNo},
            #{submittedBy, jdbcType=OTHER}, #{comment}, #{createdAt})
  </insert>
</mapper>
