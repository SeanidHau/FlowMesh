<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flowmesh.iam.repository.AuditEventRepository">
  <insert id="save" parameterType="com.flowmesh.iam.domain.audit.AuditEvent">
    INSERT INTO iam_audit_events
      (id, tenant_id, actor_user_id, action, target_type, target_id, result, trace_id, occurred_at)
    VALUES (#{id, jdbcType=OTHER}, #{tenantId}, #{actorUserId, jdbcType=OTHER}, #{action}, #{targetType}, #{targetId},
            #{result}, #{traceId}, #{occurredAt})
  </insert>
</mapper>
