# 作用：生产 profile 的 Gateway 依赖就绪策略。

management:
  health:
    redis:
      enabled: true
  endpoint:
    health:
      group:
        readiness:
          # Gateway 的分布式限流依赖 Redis；Redis 不可用时不应继续接收流量。
          include: readinessState,redis
